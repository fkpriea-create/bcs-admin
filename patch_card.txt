                // Leaderboard Button
                FilledTonalIconButton(
                    onClick = onLeaderboard,
                    modifier = Modifier.size(38.dp)
                ) {
                    Icon(androidx.compose.material.icons.filled.Leaderboard, contentDescription = "Leaderboard", modifier = Modifier.size(18.dp))
                }
                Spacer(modifier = Modifier.width(4.dp))
                // Preview Button
